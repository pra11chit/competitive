#include <bits/stdc++.h>
using namespace std;

int main() {
    int h, w;
    vector<vector<int>> vec;
    for (int i = 0; i < h; ++i) {
        for (int j = 0; j < w; ++j) {
            cin >> vec[i][j];
        }
    }
    
    return 0;
}