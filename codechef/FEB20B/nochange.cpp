#include <bits/stdc++.h>
using namespace std;

void solve(){
    //input
    int n, p;
    scanf("%d%d", &n, &p);
    vector<int> vec(n);
    for (int i=0; i<n; i++) {
        scanf("%d", &vec[i]);
    }
    //
    
}

int main() {
    int t;
    scanf("%d", &t);
    while(t--){
        solve();
    }
    return 0;
}